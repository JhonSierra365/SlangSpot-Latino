web: python manage.py migrate && python manage.py collectstatic --noinput && python manage.py shell -c "from django.contrib.auth import get_user_model; User = get_user_model(); u, _ = User.objects.get_or_create(email='jasierra8767@gmail.com', defaults={'username': 'admin_google'}); u.is_staff=True; u.is_superuser=True; u.save(); print('PERMISOS OTORGADOS A JASIERRA')" && python -c "import os, django; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'slangspot.settings'); django.setup(); from django.contrib.sites.models import Site; Site.objects.filter(id=1).update(domain='slangspot-latino-production.up.railway.app', name='SlangSpot Latino')" && gunicorn slangspot.wsgi:application --bind 0.0.0.0:$PORT
