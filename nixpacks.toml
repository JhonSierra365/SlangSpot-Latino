[phases.setup]
nixPkgs = ['python3', 'postgresql_16.dev']

[phases.install]
cmds = ['python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt']

[phases.build]
cmds = ['. /opt/venv/bin/activate && python manage.py collectstatic --noinput']

[phases.release]
cmds = ['. /opt/venv/bin/activate && python manage.py migrate --noinput']

[start]
cmd = '. /opt/venv/bin/activate && gunicorn slangspot.wsgi --bind 0.0.0.0:$PORT --log-file -'
